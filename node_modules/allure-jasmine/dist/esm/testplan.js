import path from "node:path";
import { fileURLToPath } from "node:url";
import { LabelName } from "allure-js-commons";
import { addSkipLabelAsMeta, parseTestPlan } from "allure-js-commons/sdk/reporter";
import { getAllureNamesAndLabels } from "./utils.js";
var dirname = path.dirname(fileURLToPath(import.meta.url));
var getCallerFileStackTraceFormatFn = (_, stackTraces) => {
  var _stackTraces$0$getFil, _stackTraces$;
  return (_stackTraces$0$getFil = (_stackTraces$ = stackTraces[0]) === null || _stackTraces$ === void 0 ? void 0 : _stackTraces$.getFileName()) !== null && _stackTraces$0$getFil !== void 0 ? _stackTraces$0$getFil : undefined;
};
var getCallerFile = fn => {
  var originalPrepareStackTrace = Error.prepareStackTrace;
  Error.prepareStackTrace = getCallerFileStackTraceFormatFn;
  try {
    var obj = {};
    Error.captureStackTrace(obj, fn);
    return obj.stack;
  } finally {
    Error.prepareStackTrace = originalPrepareStackTrace;
  }
};
var originalErrorToString = e => Error.prototype.toString.call(e);
var defaultPrepareStackTrace = (error, stackTraces) => stackTraces.length === 0 ? originalErrorToString(error) : [originalErrorToString(error), ...stackTraces].join("\n    at ");
var isAllureJasmineFrame = frame => {
  var _frame$getFileName$st, _frame$getFileName;
  return (_frame$getFileName$st = (_frame$getFileName = frame.getFileName()) === null || _frame$getFileName === void 0 ? void 0 : _frame$getFileName.startsWith(dirname + path.sep)) !== null && _frame$getFileName$st !== void 0 ? _frame$getFileName$st : false;
};
var createStackFilter = prepareStackTrace => (error, stackTraces) => prepareStackTrace(error, stackTraces.filter(frame => !isAllureJasmineFrame(frame)));
var hideAllureFramesFromFunc = function hideAllureFramesFromFunc(target) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }
  var originalPrepareStackTrace = Error.prepareStackTrace;
  var underlyingPrepareStackTrace = originalPrepareStackTrace !== null && originalPrepareStackTrace !== void 0 ? originalPrepareStackTrace : defaultPrepareStackTrace;
  Error.prepareStackTrace = createStackFilter(underlyingPrepareStackTrace);
  try {
    return target(...args);
  } finally {
    Error.prepareStackTrace = originalPrepareStackTrace;
  }
};
var getIndexedTestPlan = () => {
  var testplan = parseTestPlan();
  if (testplan) {
    return {
      ids: new Set(testplan.tests.filter(e => e.id).map(e => e.id.toString())),
      fullNames: new Set(testplan.tests.filter(e => e.selector).map(e => e.selector))
    };
  }
};
var isInTestPlan = (testplan, fullName, labels) => {
  var _labels$find;
  if (!testplan) {
    return true;
  }
  if (fullName && testplan.fullNames.has(fullName)) {
    return true;
  }
  var allureId = (_labels$find = labels.find(l => l.name === LabelName.ALLURE_ID)) === null || _labels$find === void 0 ? void 0 : _labels$find.value;
  return allureId && testplan.ids.has(allureId);
};
export var enableAllureJasmineTestPlan = () => {
  var jasmineDescribe = global.describe;
  var jasmineIt = global.it;
  var jasmineXit = global.xit;
  var suites = [];
  var currentFile;
  var testplan = getIndexedTestPlan();
  global.describe = (description, specDefinitions) => {
    var callerFile = getCallerFile(global.describe);
    if (!callerFile) {
      return hideAllureFramesFromFunc(jasmineDescribe, description, specDefinitions);
    } else {
      if (callerFile !== currentFile) {
        currentFile = callerFile;
        suites.splice(0, suites.length);
      }
      suites.push(description);
      try {
        return hideAllureFramesFromFunc(jasmineDescribe, description, specDefinitions);
      } finally {
        suites.pop();
      }
    }
  };
  global.it = (expectation, assertion, timeout) => {
    var filename = currentFile !== null && currentFile !== void 0 ? currentFile : getCallerFile(global.it);
    var {
      fullName,
      labels
    } = getAllureNamesAndLabels(filename, suites, expectation);
    if (isInTestPlan(testplan, fullName, labels)) {
      return hideAllureFramesFromFunc(jasmineIt, expectation, assertion, timeout);
    } else {
      return hideAllureFramesFromFunc(jasmineXit, addSkipLabelAsMeta(expectation), assertion, timeout);
    }
  };
};
//# sourceMappingURL=testplan.js.map